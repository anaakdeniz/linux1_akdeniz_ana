# git and guthub
- git is a version control system locally
- github is a cloud platform for storing git repositories also kniwn as `repos`
## commit and push
```bash
git add .
git commit -m "your commit_msg"
git push 
```

